#include<bits/stdc++.h>
using namespace std;
int main()
{
	long long min,n,s,k,t,max=0;
	cin>>t;
	for(int i=0;i<t;i++)
	{
		min=INT_MAX;
		cin>>n>>s>>k;
		int a[k],b[n],c[n];
		for(int j=0;j<n;j++)
		{
			min=INT_MIN;
			max=0;
			b[i]=i+1;
			cin>>a[j];
			if(max<a[j])
			max=a[j]
			if(b[i]==a[j])
				b[j]=INT_MAX;
			c[j]=abs(s-a[j]);
			if(if(j>k && i>=max)
			{
				break;
			}
		}
		
				
	}
}

